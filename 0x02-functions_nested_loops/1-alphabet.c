#include "main.h"
/**
 * print_alphabet - this is a function for printing the aphabets in lower case
 * Return: 0
 */
void print_alphabet(void)
{
char q;
for (q = 'a'; q <= 'z'; q++)
{
_putchar(q);
}
_putchar('\n');
}
